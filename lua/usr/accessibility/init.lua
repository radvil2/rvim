return {
	require("usr.acessibility.nvim-tree").repo,
	require("usr.acessibility.bufferline").repo,
	require("usr.acessibility.tmux-navigator").repo,
	require("usr.acessibility.trouble").repo,
	require("usr.acessibility.leap").repo,
	require("usr.acessibility.spectre").repo,
	require("usr.acessibility.gitsigns").repo,
	require("usr.acessibility.vim-illuminate").repo,
	require("usr.acessibility.todo-comments").repo,
	require("usr.acessibility.treesitter").repo,
}
